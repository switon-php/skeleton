# Tables and entities

Use this file as the app-level map between storage tables and entity classes.

- Update it before adding, renaming, or removing a table/entity pair.
- Use `—` when a table has no entity.
- Keep notes short: ownership, legacy exceptions, or migration-only context.
- This table can be empty initially; add rows once the app introduces persistent storage.

| Table | Entity | Notes |
|-------|--------|-------|

